Regler — Firebase → Firestore Database → fanen Rules → sæt ALT dette ind → Publish:

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /customers/{id} {
      allow read, write: if true;
    }
    match /pairs/{id} {
      allow read, write: if true;
    }
    match /settings/{id} {
      allow read, write: if true;
    }
    match /admins/{id} {
      allow read, write: if true;
    }
  }
}

Åbent under test. Skift senere til login.
